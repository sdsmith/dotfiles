export LC_ALL=en_US.UTF-8
export LANG=en_US.UTF-8
export LANGUAGE=en_US.UTF-8

if [ -f ~/.workdotfiles/.bash_profile ]; then
	. ~/.workdotfiles/.bash_profile
fi

if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi
